@model DateTime?

@*@if {
    @Html.TextBox("", Model.Value.ToString("yyyy-MM-dd"), new { @class = "form-control", type = "date"})
} else {
    @Html.TextBox("", "", new { @class = "form-control", type = "date"});
}*@